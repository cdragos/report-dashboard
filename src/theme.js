import {createTheme} from '@mui/material/styles'

const theme = createTheme({
  palette: {
    border: {
      base: '#F3F6F9',
    },
    background: {
      yellow: '#F6CA65',
    },
    text: {
      white: '#FFFFFF',
      link: '#005B96',
    },
  },
})

export default theme
